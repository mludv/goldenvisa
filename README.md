# Setup
Install netlify cli

  $ npm i -g netlify
  $ netlify link

Install Git LFS

  $ brew install git-lfs

Install netlify large media plugin

  $ netlify plugins:install netlify-lm-plugin
  $ netlify lm:install

